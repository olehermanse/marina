Run:

```bash
podman build --tag marina . && podman run -it -p 3000:3000 --name marina --rm marina
```

```bash
docker build --tag marina . && docker run -it -p 3000:3000 --name marina --rm marina
```
